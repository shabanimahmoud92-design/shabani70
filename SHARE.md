# لینک برنامه شیفت

## لینک دائمی

**https://shabanimahmoud92-design.github.io/shift/**

این لینک همیشه فعال است و منقضی نمی‌شود.

---

## ورود

| کاربر | رمز |
|-------|-----|
| admin | admin123 |
| hamed | hamed123 |
| mehdi | mehdi123 |

---

## به‌روزرسانی سایت بعد از تغییر کد

دوبار کلیک روی `publish.bat` — یا:

```powershell
cd D:\Mahmoud\shift-management
git add .
git commit -m "update"
git push origin main
```

سایت ۱–۲ دقیقه بعد از push خودکار به‌روز می‌شود.

---

## توجه

- لینک‌های `tiiny.site` و `netlify.app` موقتی بودند و بعد از یک ساعت منقضی شدند. از آن‌ها استفاده نکنید.
- تعطیلی‌های شرکتی در مرورگر هر نفر (localStorage) ذخیره می‌شود.
- رمزها فقط سمت مرورگر هستند؛ برای محیط عمومی حساس مناسب نیست.
